import * as Flex from '@twilio/flex-ui';

export default (flex: typeof Flex, manager: Flex.Manager) => {
  //setVisibleQueues(flex, manager);
}

function setVisibleQueues(flex: typeof Flex, manager: Flex.Manager) {
}
